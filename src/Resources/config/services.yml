services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: true

    ContaoEstateManager\OnOfficeApiImport\:
        resource: ../../
        exclude: ../../{ContaoManager,Model,Resources}
